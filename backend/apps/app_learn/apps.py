from django.apps import AppConfig


class AppLearnConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.app_learn"
    label = "app_learn"
    verbose_name = "آموزش"
