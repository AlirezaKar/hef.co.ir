from django.apps import AppConfig


class AppFinanceConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.app_finance"
    label = "app_finance"
    verbose_name = "سرمایه‌گذاری"
