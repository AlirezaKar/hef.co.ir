from django.apps import AppConfig


class AppDownloadConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.app_download"
    label = "app_download"
    verbose_name = "مرکز دانلود"
